sources:
  - name: 'snowflake source'
    database: E_COM_DB
    schema: 'E_COMM_SCHEMA'
    tables:
      - name: 'customer'

models:
  - name: "kk"
    config:
      materialized: 'table'

  - name: "kk3"
    config:
      materialized: 'view'

  - name: "user_v1"
    config:
      materialized: 'table'

  

